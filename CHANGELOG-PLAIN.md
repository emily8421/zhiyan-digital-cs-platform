# CHANGELOG-PLAIN

> Sync notice: This file is maintained by `ai-project-template` and may be overwritten when a derived project syncs template methodology.
> Do not edit it directly in derived projects; propose reusable changes in `_proposals/` and upstream them to the template repository.

本文是 `CHANGELOG.md` 的大白话同步版，按同一版本顺序解释“这版到底改善了什么”。权威版本事实仍以 `VERSION`、`CHANGELOG.md` 和 Git 历史为准；本文件只帮助人快速读懂发布影响。

## v1.56.13（2026-07-24）

让 `check-template.sh --summary` 更好读。以前摘要模式只有总数，现在会按几大块输出小计，比如 AI 入口、规则索引、核心文档、版本治理、docs scaffold、同步清单、样例项目。这样自检失败时先看是哪一块坏了，不用直接翻 1800 多条检查。默认不加参数的 CI 全量输出不变。本版也顺手归档了 3 份已经被正式规则 / 文档承接的旧提案。

## v1.56.12（2026-07-24）

小修：同步模板 dry-run 时，Windows 下不再输出一串 `LF will be replaced by CRLF` 警告。这些警告来自临时 diff，不代表同步会真的改换行；现在只在 dry-run 的 diff 环节局部关掉 autocrlf，不影响真正 `--commit` 写文件。

## v1.56.11（2026-07-24）

让派生项目同步检查的成功输出变短。以前合规文件会一条条列出来，大同步时能刷上百行；现在成功项只计数，只有异常项才逐条列，便于 AI 和人读摘要。

## v1.56.10（2026-07-24）

给同步后整理补了一段“启用项目自有版本机制”的操作清单。以前能看出项目有没有启用版本机制，但具体怎么补要现场推断；现在有固定 checklist，普通派生和领域模板也分开说明。

## v1.56.9（2026-07-23）

小修：把 `check-template` 自检时弹的一大串 "LF will be replaced by CRLF" 警告（Windows 本机才有的 git 噪声，CI 上没有）静默掉。这些警告来自自检里的临时小项目跑 git，不影响检查结果；现在本地跑 `--summary` 输出更干净（89 → 13 行）。

## v1.56.8（2026-07-23）

给 `check-template` 加了两个省事功能，主要让 AI 和本地跑自检时少烧 token、少被假失败误导。一是 `--summary`：加这个参数跑，成功只输出一行总数结论，不再刷 1800 多行 ✓（CI 仍跑全量，发布门禁不变）。二是环境守卫：当本机 Git Bash / PATH 又出问题导致根目录算错或缺关键文件时，直接早退报"环境问题、不是模板问题"，不再硬跑一千多条假失败。退出码也分清了：0 通过、1 内容失败、2 环境问题。本地快速自检以后用 `bash scripts/check-template.sh --summary`。

## v1.56.7（2026-07-23）

补了一条 Windows 本地跑 `check-template` 的最短排障路线。以后 Git Bash 起不来、PowerShell 进入 fallback、或者自检失败时，维护者能更快判断是环境入口问题还是真模板问题；成功时也只摘要命令和结果，不把完整成功日志塞进上下文。

## v1.56.6（2026-07-23）

把 PR / CI 收尾的速查入口转正。以后 AI 处理 push、开 PR、看 checks、合并、清理分支时，会先读一份短 profile，少翻长规则文件；但高风险远端动作仍然必须先问你确认。

## v1.56.5（2026-07-22）

新增这份大白话版 changelog，并把它加入模板同步范围。以后派生项目同步模板时，除了正式 `CHANGELOG.md`，也能拿到一份更容易读懂的版本说明。

## v1.56.4（2026-07-22）

改善 Windows 大批量同步模板时的操作方法：不要把满屏 CRLF warning 和长文件列表直接塞进上下文，而是用长超时、日志文件和摘要检查来判断是否真有问题。

## v1.56.3（2026-07-22）

补强复杂 Web 项目的主入口文件边界：`App.tsx` 这类主文件只负责组合和调度，业务状态、事件处理和副作用应拆到领域 hook，避免越写越大。

## v1.56.2（2026-07-22）

让模板自检失败更好排查。以前只知道某条断言失败，现在会告诉你失败文件、匹配规则和复现命令，维护者可以直接定位问题。

## v1.56.1（2026-07-22）

让 token hotspot 记录不再只是零散单条。当本地累计多份记录时，AI 会提醒生成或更新汇总，方便发现长期反复浪费上下文的地方。

## v1.56.0（2026-07-21）

新增运行时健康诊断脚本。它不只看 Node 版本对不对，还会看 Node 是从 Volta、nvm、fnm、系统 PATH 还是异常路径来的，帮助判断环境问题根因。

## v1.55.1（2026-07-21）

让前置检查能提醒 Node 主版本漂移。项目声明一个 Node 版本、机器实际跑另一个版本时，会给 warning，但不强制失败。

## v1.55.0（2026-07-20）

建立运行时版本锁定机制。项目可以写清楚要用哪个 Node / Python / Java 版本、用什么工具切换、CI 怎么校验，先用 Node 跑通第一版。

## v1.54.3（2026-07-19）

修复派生项目同步 PR squash 合并后的误报。GitHub 会给 squash 标题加 `(#N)`，现在同步检查能识别这种正常提交格式。

## v1.54.2（2026-07-18）

补充受限网络和代理配置提示。同步模板拉取失败时，不再只提示“可能是私有仓库权限”，也会提示代理和网络排查方式。

## v1.54.1（2026-07-16）

新增母模板侧的派生项目登记表。它不下发给派生项目，只帮助模板维护者知道有哪些派生项目，避免同步和回流时漏掉。

## v1.54.0（2026-07-16）

给存量派生项目增加版本机制迁移提示。检查脚本会判断老项目是否已经启用项目自有版本规则，并给出非阻断迁移建议。

## v1.53.0（2026-07-16）

新建派生项目默认从项目自有 `v0.1.0` 开始，并生成项目自己的 changelog 和版本一致性 CI，避免项目版本和模板版本混在一起。

## v1.52.5（2026-07-15）

修 Windows 兼容问题：中文规则读取时明确用 UTF-8，PowerShell 同步提交时也避免超长 pathspec 导致提交失败。

## v1.52.4（2026-07-15）

继续降低 AI 读规则和维护自检的负担：入口增加检查点节拍，任务开始前先定位规则范围，PowerShell fallback 自检也从重内容检查改成轻结构检查。

## v1.52.3（2026-07-14）

新增 Capability / Profile 索引。模板能力越来越多后，用一份人读索引说明各能力包什么时候读、输入输出是什么、不要默认全读。

## v1.52.2（2026-07-14）

澄清 Checkpoint Mode 不是“所有动作都问一遍”。低风险本地只读和已授权小改可以合并执行，高风险远端操作和破坏性动作仍单步确认。

## v1.52.1（2026-07-14）

给 Codex / AI 远端操作加防卡死协议。遇到 CI pending、网络、sandbox、权限、长输出或 GitHub 操作时，按小步汇报、失败即停处理。

## v1.52.0（2026-07-14）

新增 Web App scaffold 实验协议。候选 Web 脚手架先在真实项目或实验仓验证，不直接塞进母模板，避免把不成熟方案同步给所有项目。

## v1.51.0（2026-07-14）

新增复杂 Web / 全栈项目的结构 Profile 和 Walking Skeleton 门禁。首个业务 Sprint 前先搭好壳、目录边界、API client 和最小 smoke，避免主文件和控制器无限膨胀。

## v1.50.1（2026-07-14）

把规则读取改成“核心规则 + 任务路由 + 不确定时完整回退”。AI 不再每次无脑全量读规则，启动成本更低，但写入和不确定场景仍有硬门禁。

## v1.50.0（2026-07-13）

把 UI brief、参考分析、探索原型、视觉探索、体验 brief、正式交互设计和验收回填串成一条链，避免把探索稿直接当需求或验收事实。

## v1.49.0（2026-07-13）

新增 UI Brief Intake。做界面前先补齐参考产品、演示主线、页面结构、信息密度和视觉禁区，避免做到编码阶段才发现 UI 方向不清。

## v1.48.0（2026-07-13）

整理场景编号规则。以后顶层场景用整数编号，旧的小数编号迁移成新场景或语义子流程，降低场景索引混乱。

## v1.47.3（2026-07-13）

强化 Demo 检查。不能只看到 HTTP 200 就说 Demo 好了，还要确认页面身份、端口、代理和关键 API 确实属于当前项目。

## v1.47.2（2026-07-13）

新增 Markdown 清洁预检。PR 前检查提案和 AI 记录有没有 BOM、行尾空格、结尾空行等小格式问题，避免 CI 才爆。

## v1.47.1（2026-07-12）

修领域模板旧 `TEMPLATE-BASE.md` 的迁移兼容。老格式里的领域标准范围不会在第一次同步后丢成 TODO。

## v1.47.0（2026-07-12）

让领域模板也有自己的版本治理。领域模板从母模板同步时，可以保留自身 `VERSION` / `CHANGELOG.md`，并用领域版 `TEMPLATE-BASE.md` 记录继承关系。

## v1.46.0（2026-07-11）

普通派生项目新增双版本治理。项目自己的版本放 `VERSION`，继承的模板版本放 `TEMPLATE-BASE.md`，模板同步不再覆盖项目版本历史。

## v1.45.7（2026-07-11）

把 token hotspot 里的高频经验落成规则：同一会话里满足条件可复用已读规则，成功的长日志只保留摘要，减少重复上下文消耗。

## v1.45.6（2026-07-11）

新增领域模板实验入口。AI 可以规划“母模板 → 领域模板 → 领域派生项目”的试验线，但不改变普通派生项目的主同步路径。

## v1.45.5（2026-07-10）

让 AI 在长任务结束时主动识别 token hotspot，并询问是否记录；默认不静默写文件，也不记录隐私或完整对话。

## v1.45.4（2026-07-10）

修 Windows 新手烟测体验。Git Bash / WSL stub、前置检查和新项目完成提示更准确，减少新手误判下一步。

## v1.45.3（2026-07-10）

整理 SOP，把重复三遍的入口说明合并，保留真正有判断价值的路由和常见选择。

## v1.45.2（2026-07-10）

给几个可填模板加示例行，并给自检加防漂移断言。用户更容易填，维护者也更容易发现入口漏同步。

## v1.45.1（2026-07-10）

把新能力回写到相关文档，并修复场景编号、锚点和命令说明的漂移问题。这版主要是让文档彼此对齐。

## v1.45.0（2026-07-10）

新增项目演示 SOP 和 `show-demo` 命令。以后“启动 Demo / 看演示 / 查二维码 / 检查 Demo”有明确入口和安全边界。

## v1.44.3（2026-07-10）

新增领域模板说明文档。它解释什么时候需要母模板和普通派生项目之间的“领域模板”中间层，同时说明这是可选增强。

## v1.44.2（2026-07-09）

新增从母模板派生领域模板的场景引导，明确三层关系、创建前预检和不要把领域脚手架塞回母模板。

## v1.44.1（2026-07-09）

收紧版本影响判断。兼容、可选、默认行为不变的增强优先按 PATCH，不再因为“新增一个功能点”就升 MINOR。

## v1.44.0（2026-07-09）

同步脚本新增轻量预览参数。大版本 dry-run 可以只看摘要和风险路径，不必每次刷完整逐文件 diff stat。

## v1.43.3（2026-07-09）

修 PowerShell 同步 fallback 的参数传递问题，避免用户明明传了 `--commit`，fallback 却静默回到 dry-run。

## v1.43.2（2026-07-09）

加强 A13 同步闭环收尾。同步完成不能只说“好了”，要列完成判据矩阵、命令真实性和提案回流处理状态。

## v1.43.1（2026-07-08）

明确 Task 模板未来如果落地，应放成独立 `template-docs/task-template.md`，不要塞进 docs scaffold。

## v1.43.0（2026-07-08）

补齐 docs scaffold 的上游和辅助模板，包括输入评审、产品愿景、ADR 和 open items，让文档链路前后更完整。

## v1.42.1（2026-07-08）

优化模板版本治理：提案变多不等于要发新版，只有真正改变同步范围或模板行为的 PR 才递增版本。

## v1.42.0（2026-07-08）

把前端交互设计和 UI 原型策略从零散规则升级成独立标准与模板，并新增术语表和 docs scaffold 结构模板库。

## v1.41.1（2026-07-08）

进一步明确快速续接优先。用户只说“读取续接点”时，先走本地最小只读恢复，不展开完整规则审计。

## v1.41.0（2026-07-07）

新增快速续接模式。恢复上下文时先看 Git、版本和 handoff；不联网、不查 PR、不继续执行任务，handoff 过期就以 Git 为准。

## v1.40.0（2026-07-07）

新增需求探索原型流程。正式需求和架构定稿前，可以先用低保真页面流或静态 Mock 帮用户确认方向，但不能把原型当正式需求。

## v1.39.0（2026-07-07）

UI 型项目进入前端实现前，要选择可视化原型策略或写明豁免，减少页面结构和状态反馈到实现后才返工。

## v1.38.2（2026-07-07）

给远端 issue 本地镜像加硬门禁。维护者不能直接基于未落盘的 GitHub issue 正文做提案分析，必须先镜像到本地。

## v1.38.1（2026-07-07）

增强 GitHub issue / PR 查询稳定性。远端列表要区分 issue 和 PR，关闭或改标签前要做列表和单项状态双复核。

## v1.38.0（2026-07-07）

新增 `docs/design/*` 通用详细设计标准。复杂子系统、权限、安全、AI、外部服务、异步流程等不能只靠 04/05 粗设计。

## v1.37.0（2026-07-07）

补齐 08/09 的开发计划和验证证据标准。Sprint、Task、测试用例、验收包、缺陷回归和 handoff 边界更清楚。

## v1.36.0（2026-07-07）

补齐 06/07 的数据库和 API 契约标准。字段、接口、错误码、权限、Mock 和当前实现状态要能互相追溯。

## v1.35.0（2026-07-07）

建立文档标准分层：`ai/doc-standards/` 管细规则，`docs/00-09` 回归项目事实大纲，不再把规则手册塞进项目文档。

## v1.34.0（2026-07-07）

强化 04/05 架构与技术方案的 readiness gate。真实依赖、资源风险和技术验证在进 Sprint 前要说清楚。

## v1.33.0（2026-07-07）

强化 00-03 需求链。场景、用户需求、系统需求、阶段、验收和测试入口要能串起来，旧项目也要用映射补链。

## v1.32.0（2026-07-07）

补全文档体系生成引导，尤其是 open items 总览和专题方案讨论边界。未确认的问题不能被写成正式事实。

## v1.31.0（2026-07-07）

建立提案收件箱治理底座。远端 issue 先镜像、本地提案分批处理、状态词典和待确认项总览开始成为标准流程。

## v1.30.7（2026-07-07）

修模板自检 CI 的编码问题。去掉关键文件 BOM，避免 Linux CI 因 shebang 或版本解析失败。

## v1.30.6（2026-07-06）

补 A13 同步闭环话术。同步模板不只是跑脚本，还应包含整理、审计、提案回流和同步报告。

## v1.30.5（2026-07-06）

加强派生同步边界检查在 merge commit 场景下的提示。PR 合并后要传真正的同步提交，而不是直接检查 merge commit。

## v1.30.4（2026-07-06）

明确会话续接不能依赖 CLI 私有 session、memory 或 cache。项目恢复事实只能来自 Git、handoff、正式文档和当前用户输入。

## v1.30.3（2026-07-05）

修 `check-derived-sync.ps1` fallback 空 stderr 报错，让 Windows PowerShell 5.1 环境也能进入派生同步边界检查。

## v1.30.2（2026-07-05）

修 `sync-template.ps1` fallback 空 stderr 报错，补齐 Windows PowerShell 5.1 的实机兼容性缺口。

## v1.30.1（2026-07-05）

加强 PowerShell 同步 fallback 的 UTF-8 处理，避免中文 Markdown、JSON 和文件名在 Windows 代码页下乱码。

## v1.30.0（2026-07-05）

新增技术路线与环境支撑评估。真实依赖进入编码前，要先评估版本、安装、最小运行、Docker、数据库、模型、网络和资源。

## v1.29.0（2026-07-05）

新增前端交互设计规则。复杂 UI 项目前端开发前要补交互设计，前端隐藏按钮不能替代后端权限。

## v1.28.0（2026-07-05）

新增文档评估机制。关键阶段转换前可以输出 Go / Conditional Go / No-Go，避免文档没准备好就进入下一阶段。

## v1.27.9（2026-07-05）

待人工确认项升级为结构化表格：AI 建议、依据、备选和影响都要写清楚，但仍不能替用户做最终确认。

## v1.27.8（2026-07-04）

统一用户原始输入入口到 `docs/inputs/`，并在生成产品愿景和 00-09 前加愿景就绪评审。

## v1.27.7（2026-07-04）

把模板仓 CI 和派生项目 CI 分开。派生项目普通 PR 不应误跑模板仓完整自检。

## v1.27.6（2026-07-04）

把方法论同步升级成标准闭环：同步、边界验证、同步后整理、文档审计、项目验证建议和同步报告留痕。

## v1.27.5（2026-07-04）

补齐 GitHub issue 提案收件箱和维护者 triage 流程，让派生项目通过 issue 回流的建议也能纳入 C1 处理。

## v1.27.4（2026-07-04）

整理 `check-template.sh` 的结构，先分组和收敛重复断言，降低后续维护成本。

## v1.27.3（2026-07-04）

强化跨 AI CLI 写入前确认。AI 改文件前要说明目的、范围、文件、风险和验证，工具配置和 Git 审计作为兜底。

## v1.27.2（2026-07-04）

整理 Scenario Guide，让场景层承接实现生命周期，并补齐 HELP、能力索引和维护者场景说明。

## v1.27.1（2026-07-04）

让 08 开发计划、09 验证计划和 A9 规划 Prompt 承接实现生命周期规则，开始补 Sprint / Task / 验证闭环。

## v1.27.0（2026-07-04）

新增实现生命周期规则，补上从文档体系到编码执行、测试验证、提交 PR 和验收留痕的完整链条。

## v1.26.2（2026-07-03）

补齐 scripts 说明，明确 Bash 脚本是完整权威检查，PowerShell 是结构性 fallback，fallback 通过不等于完整自检通过。

## v1.26.1（2026-07-03）

把派生同步运行记录从 `docs/archive/` 挪到 `sync-records/`，避免把同步过程记录混进项目事实文档。

## v1.26.0（2026-07-03）

强化会话续接规则。被动中断或换 CLI 接手时，以 Git 为唯一可信锚点，handoff 只能辅助参考。

## v1.25.0（2026-07-03）

建立派生项目向模板回流提案和反馈的标准渠道，新增 `submit-proposal` / `submit-feedback` 两个命令和来源标识规则。

## v1.24.5（2026-07-03）

加入多 AI 会话并发操作约定。多个会话同时改同一仓库时，建议用独立 worktree，避免 commit 落错分支。

## v1.24.4（2026-07-03）

重新定位 `INIT-PROMPT.md`，把它从“Prompt 模板索引”改成“AI 任务启动入口”，文件名、标题和内容更一致。

## v1.24.3（2026-07-03）

派生同步检查增加 README 版本号非阻断提醒。README 是项目专属，不会被模板覆盖，但同步后若版本声明滞后会提示人工核对。

## v1.24.2（2026-07-03）

建议在 PRD 阶段路线图顶部增加“双维度总览表”，让功能范围和交付物形态一眼看清。

## v1.24.1（2026-07-02）

完成 v1.24 测试基础设施收官。新增端到端回归清单、脚本和报告模板，MINOR / MAJOR 发布前有更强回归门。

## v1.24.0（2026-07-02）

新增批量同步派生项目脚本和 C8 场景。维护者可以在父目录下 dry-run 或 commit 同步多个派生项目。

## v1.23.7（2026-07-02）

给文档生命周期规则加阅读地图，并精简 global-rules 的重复流程说明，让读者更快知道该看哪一节。

## v1.23.6（2026-07-02）

让 SOP 和 Scenario Guide 使用同一套场景码。SOP 看命令，Scenario Guide 看完整剧本，两者能互相跳转。

## v1.23.5（2026-07-02）

给 Scenario Guide 增加场景速查索引，按角色列触发说法和一句话说明，找场景更快。

## v1.23.4（2026-07-02）

优化 `template-docs` 三份手册的结构和导航，让新手、方法论和场景说明更好读。

## v1.23.3（2026-07-02）

给关键文件夹补 README，包括 `template-docs/`、`scripts/`、`ai/` 和代码骨架目录。主要是让模板仓本身更好逛，不改变下游同步规则。

## v1.23.2（2026-07-02）

把 `git-guide.md` 改成按场景组织。用户不需要先理解 Git 主题分类，而是按“派生日常、模板维护、派生同步、新建项目”找操作。

## v1.23.1（2026-07-02）

重构维护者和贡献者文档。`MAINTAINERS.md` 更像维护者工作手册，`CONTRIBUTING.md` 更像贡献流程说明，README 也区分使用者和维护者。

## v1.23.0（2026-07-02）

启动文档体系重构第一批：把 beginner guide 和 docs README 改成读者导向，先讲输入、输出、00-09 文档各自干什么，再讲规则。

## v1.22.5（2026-07-02）

基于真实派生项目同步发现的问题修 PowerShell fallback 空对象 bug，并放宽同步检查对未跟踪项目内容的误拦截；同时规范派生项目 README 结构。

## v1.22.4（2026-07-02）

把 README 开头和新手指南改得更通俗。README 讲清模板能做什么，新手指南从 15 章压到 5 章，减少第一次阅读压力。

## v1.22.3（2026-07-02）

整理入口文档的目录速览和旧引用，补 `_archive/`、`tasks/`、`ai/prompts/` 等路径说明，让 README、SOP、CONTRIBUTING 和 MAINTAINERS 不再互相漂移。

## v1.22.2（2026-07-01）

把 `INIT-PROMPT.md` 瘦身成入口指针，不再复制一张很长的场景 / 命令 / Prompt 明细表，避免和 SOP、commands README 重复维护。

## v1.22.1（2026-07-01）

把 README 控制在更轻的入口页：快速开始、当前版本和目录速览留在 README，常用命令挪到 SOP，轻量项目路径挪到 docs README。

## v1.22.0（2026-07-01）

新增场景引导层和 `/run scenario`。用户描述一个场景，AI 先按角色和路径给出“做什么、为什么、机器执行什么”的计划，再继续执行。

## v1.21.3（2026-07-01）

给 PowerShell 环境补原生同步 fallback。Git Bash 起不来时，仍能做模板同步预览 / commit 和派生同步边界检查。

## v1.21.2（2026-06-30）

增强文档体系审计 Prompt。审计时要区分事实问题、追溯断点、规范基线差异和部署可行性缺口，避免把旧项目兼容差异误判成业务错误。

## v1.21.1（2026-06-30）

优化新手入口顺序：先检查环境，再新建项目、采集环境、评审输入和生成文档；缺环境时直接给下一步安装建议。

## v1.21.0（2026-06-29）

新增派生同步运行记录模板。每次真实同步模板后，记录版本、命令、检查结果、问题和可回流优化点，后续整理和回流更有依据。

## v1.20.0（2026-06-29）

引入 `ai/doc-standards/` 作为文档撰写规范镜像，让项目事实文档和规则标准分开维护。

## v1.19.0（2026-06-29）

新增 AI 快捷命令路由和会话续接规则。常见任务可以通过 `/run ...` 或自然语言路由到对应 Prompt，新窗口也能靠 `.ai/session-handoff.md` 恢复上下文。

## v1.18.3（2026-06-28）

让模板自检动态读取 `VERSION` 检查 changelog，并清理同步文档里的个人账号信息；README 也把使用者命令和维护者命令分开。

## v1.18.2（2026-06-28）

给 `check-template.sh` 增加“防文档滞后”断言，确保脚本机制变了以后，git-guide、SOP、MAINTAINERS 等人读文档也跟上。

## v1.18.1（2026-06-28）

补上 v1.17 / v1.18 落地后漏改的人读操作文档，说明 `_scaffold` 规范镜像和 docs-system-audit 闭环，避免脚本懂、人不懂。

## v1.18.0（2026-06-28）

新增 `docs/_scaffold/` 规范镜像机制。派生项目同步模板时拿到只读的 00-09 撰写规范副本，但不会覆盖自己的项目事实文档。

## v1.17.0（2026-06-28）

新增 docs-system-audit Prompt，用来在项目文档成型后回头检查追溯链、横切一致、变更传播和可行性，先出报告不直接改文件。

## v1.16.2（2026-06-27）

补充模板维护纪律：无论是已有提案还是对话中新提出的模板改动，都要先切维护分支、维护提案记录，合并后再归档。

## v1.16.1（2026-06-27）

把新手、环境、AI CLI、烟测、方法论等元文档集中迁到 `template-docs/`，避免根目录堆一堆大说明文件。

## v1.16.0（2026-06-27）

新增 AI CLI 安装与配置说明，把 Claude CLI / Codex CLI 和公司中转站配置顺序拆清楚，避免新手不知道 AI 工具怎么接入。

## v1.15.1（2026-06-27）

修正公司中转站说明边界：官方 CLI 安装 / 登录是一回事，内网模型代理配置是另一回事，文档不再混写。

## v1.15.0（2026-06-27）

把 Claude CLI / Codex CLI 纳入环境准备说明，并补公司中转站入口；烟测也明确暂不覆盖 AI CLI 登录和内网代理配置。

## v1.14.0（2026-06-27）

给环境准备文档补“每个工具是什么、为什么装、什么时候可跳过”，让新手不只是照着软件清单安装。

## v1.13.0（2026-06-27）

新增烟测报告模板，并修一批新手烟测问题：`gh` 改成条件必需、Git Bash 路径更明确、新项目没有 Git 身份时也能本地初始化。

## v1.12.0（2026-06-27）

新增 Windows 新手烟测操作单，串起环境检查、本地建项目、环境采集和文档入口验证，并明确 PowerShell / Git Bash 的边界。

## v1.11.0（2026-06-27）

新增环境准备手册、前置检查脚本和一键安装脚本。新手可以先检查 Git、Git Bash、gh、Node、Python、VS Code 等基础工具。

## v1.10.0（2026-06-27）

新增新手指南和模板方法论说明，把“第一次用该看什么”和“模板为什么这么设计”从 README 中拆出来。

## v1.9.0（2026-06-26）

拆分 Prompt Library。`INIT-PROMPT.md` 变轻量索引，完整 Prompt 移到 `ai/prompts/` 并按任务类型分类。

## v1.8.0（2026-06-26）

新增文档生命周期规则。它定义输入材料、文档剖面、追溯链、变更传播和外部文档接入，让 AI 生成文档有上游依据。

## v1.7.1（2026-06-25）

跟进双维度阶段规则，修派生 README 和示例项目，避免继续把 Phase1 默认写成 MVP。

## v1.7.0（2026-06-24）

新增阶段双维度：功能范围是 P1 / P2 / 愿景，交付物形态是 Demo / MVP / 产品。Phase1 不再默认等于 MVP。

## v1.6.9（2026-06-24）

新增派生项目同步后的边界检查脚本，明确派生项目检查用 `check-derived-sync`，不要误跑模板仓完整自检。

## v1.6.8（2026-06-24）

新增同步后项目整理 Prompt。派生项目同步方法论后，要检查 docs 分区、README、项目规则和环境资源约束是否需要迁移补齐。

## v1.6.7（2026-06-24）

给所有会下行同步的 Markdown 文件加 Sync notice，并强调派生项目根 README 是项目专属，不会被模板同步覆盖。

## v1.6.6（2026-06-24）

README 瘦身，把维护细节迁到 `MAINTAINERS.md`，完整版本历史迁到 `CHANGELOG.md`，并新增 `docs/README.md` 约束 docs 分区。

## v1.6.5（2026-06-23）

新增 GitHub Actions 自检、`template-sync.json` 同步清单、PowerShell 入口和新项目烟测。模板发布开始有更明确的自动检查。

## v1.6.4（2026-06-23）

新增 `SOP.md`，把新建项目、初始化 docs、环境采集、Sprint 执行、审查、同步和回流这些常见操作做成场景索引。

## v1.6.3（2026-06-23）

修正 `sync-template.sh --dry-run` 的差异方向。预览现在和实际覆盖方向一致，不会把模板新增误显示成删除。

## v1.6.2（2026-06-23）

把新建 / 同步项目的标准操作固化成可复制 Prompt，并让同步 Prompt 运行时读取当前模板版本，不再写死版本号。

## v1.6.1（2026-06-23）

增强同步安全：派生项目同步前会先比对远端最新版同步脚本，发现本地脚本旧就停下，避免旧脚本漏同步新文件。

## v1.6.0（2026-06-23）

建立运行环境资源约束机制。先回答“这台机器跑不跑得动”，为后续技术方案和本机演示提供依据。

## v1.5（2026-06-22）

明确 `frontend/` 是否启用由项目演示形态决定，并建立模板优化反馈机制：派生项目的通用经验先写提案，再回流模板仓。

## v1.4（2026-06-19）

新增文档演进规则和 09 验证支柱。设计文档按完整骨架加阶段标签积累演进，不再只生成当前阶段的薄片。

## v1.3（2026-06-17）

把数据库 / API 文档改成按项目形态启用，并修正样例和 checklist，避免无 DB / 无 API 项目还被强行要求写 06 / 07。

## v1.2（2026-06-17）

把项目形态与文档裁剪前置为初始化必填。先判断有没有持久化、对外接口和前端，再决定 06 / 07 是否保留。

## v1.1（2026-06-16）

给 Cursor 入口加 `alwaysApply`，让 06 / 07 可按项目形态省略，并补早期版本戳、验收 checklist 和 docs 骨架。

## v1.0

模板初始版本：建立 AI 项目模板的基础目录、文档和规则入口，详细设计说明保留在早期归档里。
