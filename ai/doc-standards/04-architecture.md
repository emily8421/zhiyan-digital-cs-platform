# 04 系统架构（Architecture）

> **文档定位**：定义系统边界、架构视图、模块职责、关键流程、运行拓扑和架构决策。本文回答“系统如何组织”，不写具体依赖版本、表字段、接口字段或 Sprint 实施步骤。
>
> **上游输入**：`docs/03-prd.md`、`docs/02-srs.md`、`docs/env/local-env.md`、`ai/project-rules.md`、`ai/doc-standards/04-architecture.md`。
>
> **下游输出**：为 `docs/05-tech-spec.md`、`docs/06-db-design.md`、`docs/07-api-spec.md`、`docs/design/*`、`docs/08-dev-plan.md`、`docs/09-verification.md` 提供架构边界。

## 0. 文档元信息

【撰写提要：记录本文输入来源、覆盖范围、当前状态和最后更新时间；不得把模板占位内容当作项目事实。】
| 项 | 内容 |
|---|---|
| 输入来源 |  |
| 覆盖功能 / REQ | F-001 / REQ-001... |
| 当前状态 | 草稿 / 待人工确认 / 已确认 |
| 最后更新 | YYYY-MM-DD |

## 1. 架构目标与约束

【撰写提要：说明架构要优先满足什么，以及必须遵守的 Phase、资源、交付物形态、项目形态裁剪、禁止项和权威源约束。】

| 约束项 | 内容 | 权威源 | 下游影响 |
|---|---|---|---|
| 架构目标 |  | `docs/03-prd.md` | 04-09 |
| 当前 Phase / 交付物形态 | Phase1 / Demo、MVP、产品 | `docs/03-prd.md` §3、`ai/project-rules.md` §1 | 05 / 08 / 09 |
| 运行环境约束 | 见 `docs/env/local-env.md` 与 `ai/project-rules.md` §2.5 | `docs/env/local-env.md` | 05 / 09 |
| 项目形态裁剪 | 见 `ai/project-rules.md` §3 | `ai/project-rules.md` | 05 / 06 / 07 / frontend |
| 禁止项 / 非目标 |  | `docs/00-scenario.md`、`docs/03-prd.md` | 04-09 / Sprint 禁止事项 |

## 2. 系统上下文图

【撰写提要：用文字、ASCII 或 Mermaid 描述系统与用户、外部系统、数据源、运行环境的关系；必须标明真实、Mock、候选、默认关闭或禁止的外部交互。】

```text
[用户 / 外部系统] -> [本系统] -> [输出 / 下游]
```

| 外部参与者 / 系统 | 交互 | 输入 | 输出 | 状态 | 信任边界 / 风险 |
|---|---|---|---|---|---|
|  |  |  |  | 真实 / Mock / 候选 / 默认关闭 / 禁止 |  |

## 3. 容器 / 进程 / 组件视图

【撰写提要：列出主要运行单元、职责、通信方式、部署位置。Lean 项目可只写单进程或脚本结构。】

| COMP-ID | 组件 / 进程 | 职责 | 部署位置 | 通信方式 | 阶段 | 状态 | 关联 REQ / NFR |
|---|---|---|---|---|---|---|---|
| COMP-001 |  |  | 本机 / 服务器 / Docker / 浏览器 |  | [P1] | 骨架 / P1-已设计 / Mock / 默认关闭 | REQ-001 / NFR-001 |

## 4. 模块划分

【撰写提要：每个模块职责要清晰，避免把无关能力塞入当前 Phase。非平凡模块应指向 `docs/design/<子系统>.md`；页面型系统应标出前端入口 / 页面模块，并指向 `docs/design/frontend-interaction.md` 或对应 `docs/design/*interaction*.md`。】

| MOD-ID | 模块 | 职责 | 输入 | 输出 | 边界 / 不负责 | 关联组件 | 关联设计 |
|---|---|---|---|---|---|---|---|
| MOD-001 |  |  |  |  |  | COMP-001 | `docs/design/...` |

## 5. 关键流程与数据流

【撰写提要：描述核心用户场景如何穿过模块；每条流程应能回到 03 功能或 02 REQ，并覆盖成功、异常、降级、权限拒绝和外部服务不可用路径。】

### Flow-001：

1. 触发：
2. 主要步骤：
3. 成功结果：
4. 异常路径：
5. 降级 / Mock 路径：
6. 权限拒绝路径：
7. 外部服务不可用路径：
8. 关联 REQ / 功能：
9. 关联 API / 数据 / TC：

## 6. 部署 / 运行拓扑约束

【撰写提要：说明本机、服务器、容器、端口、进程、资源边界、持久化、外部依赖和可降级部分。Demo / MVP 默认优先本机可运行。】

| 环境 | 拓扑 | 端口 / 进程 | 资源要求 | 持久化 | 外部依赖 | 降级 / Mock | 备注 |
|---|---|---|---|---|---|---|---|
| 本机 Demo |  |  |  | 无 / 文件 / DB |  |  |  |
| 服务器 / 远程 |  |  |  |  |  |  |  |

## 7. 架构决策与取舍

【撰写提要：记录为什么这样设计、拒绝了哪些替代方案；横切事实应引用 `docs/decisions/` 或 `ai/project-rules.md`，不要各文档重复定义。】

| ADR-ID | 决策 | 状态 | 适用 Phase | 理由 | 替代方案 | 取舍影响 | 验证方式 / 证据 |
|---|---|---|---|---|---|---|---|
| ADR-001 |  | 候选 / 已确认 / 默认关闭 / 废弃 / 禁止 | Phase1 |  |  |  | 命令 / Spike / PoC / TC / 待验证 |

## 8. REQ / 功能 → 模块追溯矩阵

【撰写提要：填写本章节的项目事实；依据上游文档和对应 ai/doc-standards，不得新增未确认内容。】
| REQ / NFR / 功能 | Phase | COMP-ID | MOD-ID | Flow-ID | 详细设计 | 覆盖状态 |
|---|---|---|---|---|---|---|
| REQ-001 / F-001 | Phase1 | COMP-001 | MOD-001 | Flow-001 | `docs/design/...` | 已覆盖 / 待补 / 不适用 |

## 9. 架构视图检查表

【撰写提要：生成或定稿前自检 04 是否覆盖必要视图；缺失项应进入待确认项或修订计划。】

| 视图 | 必查项 | 通过标准 | 缺失修复 |
|---|---|---|---|
| 系统上下文 | 用户、外部系统、核心服务、边界 | 明确哪些外部系统是真实、Mock、候选或默认关闭 | 补上下文图或边界说明 |
| 组件 / 容器 | 前端、后端、数据、适配层、测试入口 | 每个组件有职责、部署位置、通信方式、阶段、状态和覆盖 REQ | 补组件表或模块图 |
| 关键流程 | 主流程、异常流程、降级流程、权限拒绝 | 流程能关联 API、服务、数据对象、TC 或验收口径 | 补流程图和映射 |
| 运行拓扑 | 本机、服务器、Docker、外部服务 | 明确端口、资源、持久化、可选依赖和降级路径 | 补拓扑和资源约束 |
| ADR / 决策 | 关键技术 / 交付 / 安全决策 | 有状态、理由、备选、取舍影响和验证方式 | 补 ADR 字段 |

## 10. 待人工确认项

【撰写提要：记录本阶段仍需人工确认的事项；必须包含 AI 建议、建议依据、备选方案和取舍影响，确认前不得写成事实。】
> 待确认项必须保留 AI 建议、建议依据、备选方案和取舍影响；AI 建议不等于用户确认。

| ID | 待确认项 | AI 建议 | 建议依据 | 备选方案 | 取舍影响 / 阻塞关系 |
|---|---|---|---|---|---|
| C-001 |  |  |  |  |  |
