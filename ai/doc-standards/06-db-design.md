# 06 数据库设计（Database Design）

> **文档定位**：当项目涉及持久化数据库时，定义数据对象、概念模型、表字段、索引约束、迁移 / seed / 回滚、数据安全留存和实现状态差异。本文回答“数据如何持久化，以及当前实现是否等价于目标设计”。
>
> **适用条件**：仅当 `ai/project-rules.md` §3 声明 `docs/06-db-design.md：保留` 时使用。若无持久化数据库，初始化时应删除本文并在 §3 留下省略原因；浏览器 localStorage / IndexedDB / sessionStorage 等非数据库存储写入 `docs/05-tech-spec.md`。
>
> **上游输入**：`docs/02-srs.md`、`docs/03-prd.md`、`docs/04-architecture.md`、`docs/05-tech-spec.md`。
>
> **下游输出**：约束模型、迁移、seed / fixture、查询逻辑、API 字段来源、错误映射、测试数据和 Phase 升级门槛。

## 0. 文档元信息

【撰写提要：记录本文输入来源、覆盖范围、当前状态和最后更新时间；不得把模板占位内容当作项目事实。】
| 项 | 内容 |
|---|---|
| 保留 / 省略决策 | 保留 / 省略 |
| 决策来源 | `ai/project-rules.md` §3 |
| 覆盖 REQ / 模块 |  |
| 当前状态 | 草稿 / 待人工确认 / 已确认 |
| 最后更新 | YYYY-MM-DD |

## 1. 数据需求概览

【撰写提要：说明哪些 REQ / NFR 需要持久化、为什么需要数据库、数据生命周期、敏感性、访问边界与下游接口 / 模块。】

| 数据对象 | 来源 REQ / NFR | 用途 | 生命周期 | 敏感性 | 访问边界 | 下游接口 / 模块 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 2. 概念模型

【撰写提要：用实体与关系描述数据，不急于写字段；确保每个实体有需求来源。】

| 实体 | 描述 | 关联 REQ / NFR | 关系 | 阶段 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

## 3. 表清单

【撰写提要：每张表必须能追溯到 REQ 或非功能约束；不得创建“可能以后有用”的表。】

| 表名 | 阶段 | 状态 | 用途 | 来源 REQ / NFR / 约束 | 数据对象 | 备注 |
|---|---|---|---|---|---|---|
| app_records | [P1] | 骨架 / P1-已设计 |  | REQ-001 |  |  |

## 4. 目标结构与当前实现对照

【撰写提要：区分目标存储、当前实现、Mock / Demo 差异、迁移状态和验证项；不得把未来表或 Mock 数据写成已实现。】

| 数据对象 / 表 | 来源 REQ | 目标存储 | 当前实现 | 契约状态 | 迁移状态 | Seed / fixture 状态 | 验证项 |
|---|---|---|---|---|---|---|---|
| app_records | REQ-001 |  |  | 目标设计 / 当前实现 / Mock / 已验证 | 未迁移 / 已迁移 / 不适用 | 有 / 无 / 不适用 | TC-001 |

## 5. 表结构字段级契约

【撰写提要：当前阶段表写字段、类型、必填、默认值、约束、来源、敏感性和留存 / 删除；远期表只保留骨架，待阶段到来再细化。】

### app_records

| 字段 | 类型 | 必填 | 默认值 | 约束 | 说明 | 来源 REQ / NFR / 约束 | 敏感性 | 留存 / 删除 |
|---|---|---|---|---|---|---|---|---|
| id |  | 是 |  | 主键 |  |  | 非敏感 / 敏感 / 待确认 |  |

## 6. 索引、约束与关系

【撰写提要：说明主键、唯一约束、外键、常用查询索引、全文 / 向量索引、级联行为和查询 / 写入影响。】

| 表 | 索引 / 约束 | 字段 | 类型 | 用途 | 查询 / 写入影响 | 阶段 | 状态 |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |

## 7. 数据迁移、初始化与回滚

【撰写提要：说明迁移工具、迁移文件命名、seed / fixture 来源、回滚策略、环境差异和验证入口；无真实数据库时写 fixture / seed 来源轻量版本。】

| 对象 / 表 | 迁移路径 | Seed / fixture 来源 | 回滚策略 | 环境差异 | 验证入口 / TC | 状态 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 8. 数据安全与留存

【撰写提要：说明敏感字段、脱敏、加密、访问控制、日志留存、删除策略和外部传输限制。】

| 数据 / 表 / 字段 | 敏感性 | 访问控制 | 脱敏 / 加密 | 留存 / 删除 | 外部传输限制 | 验证入口 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 9. DB / API 交叉追溯

【撰写提要：记录 API 字段到 DB 字段、约束到错误码、权限到查询过滤的映射；权限边界不得只依赖前端隐藏或禁用。】

| API-ID | 请求 / 响应字段 | 数据来源 | 表 / 字段 | 计算逻辑 | 敏感性 | 脱敏 / 过滤 |
|---|---|---|---|---|---|---|
| API-001 |  |  |  |  |  |  |

| DB / 业务约束 | 触发接口 | 错误码 | 用户可见信息 | 是否可重试 | 日志 / 审计 |
|---|---|---|---|---|---|
|  | API-001 |  |  | 是 / 否 |  |

## 10. REQ → 表 / 字段追溯矩阵

【撰写提要：填写本章节的项目事实；依据上游文档和对应 ai/doc-standards，不得新增未确认内容。】
| REQ / NFR / 约束 | 数据对象 | 表 | 字段 / 索引 / 约束 | 覆盖说明 | 状态 |
|---|---|---|---|---|---|
| REQ-001 |  | app_records |  |  |  |

## 11. 待人工确认项

【撰写提要：记录本阶段仍需人工确认的事项；必须包含 AI 建议、建议依据、备选方案和取舍影响，确认前不得写成事实。】
> 待确认项必须保留 AI 建议、建议依据、备选方案和取舍影响；AI 建议不等于用户确认。

| ID | 待确认项 | AI 建议 | 建议依据 | 备选方案 | 取舍影响 / 阻塞关系 |
|---|---|---|---|---|---|
| C-001 |  |  |  |  |  |
