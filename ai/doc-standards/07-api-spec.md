# 07 API / 接口设计（API Specification）

> **文档定位**：当项目对外暴露接口、命令、SDK、事件或集成契约时，定义统一约定、接口清单、请求响应、错误码、权限和兼容策略。
>
> **适用条件**：当 `ai/project-rules.md` §3 声明 `docs/07-api-spec.md：保留` 时使用。CLI / 本地脚本也可保留本文，用于描述命令、参数、输出和退出码；若完全无对外接口，初始化时应删除本文并在 §3 留下省略原因。
>
> **上游输入**：`docs/02-srs.md`、`docs/03-prd.md`、`docs/04-architecture.md`、`docs/05-tech-spec.md`。
>
> **下游输出**：约束 API 层、CLI 命令、SDK、前端调用、集成测试和验收用例。

## 0. 文档元信息

| 项 | 内容 |
|---|---|
| 保留 / 省略决策 | 保留 / 省略 |
| 接口形态 | REST / GraphQL / CLI / SDK / Event / Internal Contract / Other |
| 覆盖 REQ / 模块 |  |
| 当前状态 | 草稿 / 待人工确认 / 已确认 |
| 最后更新 | YYYY-MM-DD |

## 1. 统一约定

**撰写提要**：说明协议、编码、认证、幂等、分页、时间格式、错误格式、版本策略。CLI 项目写命令格式、退出码、stdout/stderr 约定。

- 协议 / 命令形态：
- 数据格式：
- 鉴权 / 权限：
- 错误格式：
- 版本策略：

## 2. 接口 / 命令清单

**撰写提要**：每个接口必须对应 REQ 或明确的非功能约束；不得创建孤立接口。

| 接口 ID | 方法 / 命令 | 路径 / 名称 | 用途 | 阶段 | 状态 | 来源 REQ |
|---|---|---|---|---|---|---|
| API-001 | POST | /process |  | [P1] | 骨架 / P1-已设计 | REQ-001 |

## 3. 请求 / 输入契约

**撰写提要**：定义参数、类型、必填、校验规则和示例。当前阶段接口写完整，远期接口保留骨架。

### API-001：

| 字段 / 参数 | 类型 | 必填 | 校验 | 示例 | 说明 |
|---|---|---|---|---|---|
|  |  | 是 / 否 |  |  |  |

## 4. 响应 / 输出契约

**撰写提要**：定义成功响应、失败响应、输出字段、退出码或事件格式。

### API-001：

成功示例：

```json
{}
```

失败示例：

```json
{}
```

## 5. 错误码与异常处理

**撰写提要**：列出错误码、触发条件、用户可见信息和处理建议。

| 错误码 / 退出码 | 触发条件 | 用户可见信息 | HTTP 状态 / 退出码 | 处理建议 |
|---|---|---|---|---|
| VALIDATION_ERROR |  |  | 400 / 2 |  |

## 6. 权限、安全与限流

**撰写提要**：说明身份认证、权限边界、敏感字段、速率限制、审计日志；无则写“当前阶段无已确认要求”。

| 接口 | 权限要求 | 敏感数据 | 限流 / 审计 | 备注 |
|---|---|---|---|---|
| API-001 |  |  |  |  |

## 7. 兼容性与版本演进

**撰写提要**：说明接口变更、版本兼容、弃用策略和客户端影响。

- 兼容原则：
- 破坏性变更处理：
- 弃用策略：

## 8. REQ → 接口追溯矩阵

| REQ / 功能 | 接口 / 命令 | 覆盖说明 | 状态 |
|---|---|---|---|
| REQ-001 / F-001 | API-001 |  |  |

## 9. 待人工确认项

-