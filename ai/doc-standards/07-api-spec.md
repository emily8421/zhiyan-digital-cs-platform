# 07 API / 接口设计（API Specification）

> **文档定位**：当项目对外暴露接口、命令、SDK、事件、Webhook、文件导入导出、异步任务或集成契约时，定义 API-ID、请求 / 响应 / 错误 / 权限 / 兼容契约、状态机和追溯矩阵。
>
> **适用条件**：当 `ai/project-rules.md` §3 声明 `docs/07-api-spec.md：保留` 时使用。CLI / 本地脚本也可保留本文，用于描述命令、参数、输出和退出码；若完全无对外接口，初始化时应删除本文并在 §3 留下省略原因。
>
> **上游输入**：`docs/02-srs.md`、`docs/03-prd.md`、`docs/04-architecture.md`、`docs/05-tech-spec.md`。
>
> **下游输出**：约束 API 层、CLI 命令、SDK、前端调用、Service、权限检查、错误处理、集成测试、验收用例和 Phase 升级门槛。

## 0. 文档元信息

【撰写提要：记录本文输入来源、覆盖范围、当前状态和最后更新时间；不得把模板占位内容当作项目事实。】
| 项 | 内容 |
|---|---|
| 保留 / 省略决策 | 保留 / 省略 |
| 接口形态 | REST / GraphQL / CLI / SDK / Event / Internal Contract / Other |
| 覆盖 REQ / 模块 |  |
| 当前状态 | 草稿 / 待人工确认 / 已确认 |
| 最后更新 | YYYY-MM-DD |

## 1. 统一约定

【撰写提要：说明协议、编码、认证、幂等、分页、时间格式、错误格式、版本策略。CLI 项目写命令格式、退出码、stdout/stderr 约定。】

- 协议 / 命令形态：
- 数据格式：
- 鉴权 / 权限：
- 错误格式：
- 版本策略：

## 2. 接口 / 命令清单

【撰写提要：每个接口必须对应 REQ 或明确的非功能约束；不得创建孤立接口。】

| API-ID | 方法 / 命令 | 路径 / 名称 | 用途 | 阶段 | 状态 | 来源 REQ / NFR | 关联模块 |
|---|---|---|---|---|---|---|---|
| API-001 | POST | /process |  | [P1] | 骨架 / P1-已设计 | REQ-001 | MOD-001 |

## 3. Endpoint contract matrix

【撰写提要：当前 Phase 或即将进入 Phase 的 API 必须有稳定 API-ID 和契约状态；草案 / 候选 / 默认关闭 API 不得直接作为开发任务依据，除非已人工确认或先做 Spike / PoC。】

| API-ID | 契约状态 | 请求契约 | 响应契约 | 错误契约 | 权限契约 | 兼容性 | 验证项 | 是否可实现 |
|---|---|---|---|---|---|---|---|---|
| API-001 | 草案 / 当前实现 / 已验证 / Mock | 完整 / 缺失 | 完整 / 缺失 | 完整 / 缺失 | 完整 / 缺失 | 内部 / 对外稳定 | TC-001 | 是 / 否 / 待确认 |

## 4. 请求 / 输入契约

【撰写提要：定义参数、位置、类型、必填、校验规则、示例和来源。当前阶段接口写完整，远期接口保留骨架。】

### API-001：

| 字段 / 参数 | 位置 | 类型 | 必填 | 校验 | 示例 | 说明 | 来源 REQ / NFR |
|---|---|---|---|---|---|---|---|
|  | body / query / path / header |  | 是 / 否 |  |  |  |  |

## 5. 响应 / 输出契约

【撰写提要：定义成功响应、失败响应、输出字段、退出码或事件格式，并标明数据来源、敏感性和脱敏 / 过滤策略。】

### API-001：

成功示例：

```json
{}
```

失败示例：

```json
{}
```

| API-ID | 字段 | 类型 | 必填 | 示例 | 说明 | 数据来源 / 表字段 | 敏感性 | 脱敏 / 过滤 |
|---|---|---|---|---|---|---|---|---|
| API-001 |  |  | 是 / 否 |  |  |  | 非敏感 / 敏感 / 待确认 |  |

## 6. 错误码与异常处理

【撰写提要：列出错误码、触发条件、用户可见信息、客户端处理、日志 / 审计和是否可重试。】

| 错误码 / 退出码 | API-ID | HTTP 状态 / 退出码 | 触发条件 | 用户可见信息 | 客户端处理 | 日志 / 审计 | 是否可重试 |
|---|---|---|---|---|---|---|---|
| VALIDATION_ERROR | API-001 | 400 / 2 |  |  |  |  | 否 |

## 7. 权限、安全与限流

【撰写提要：说明鉴权、空间 / 租户边界、资源权限、敏感字段、速率限制、审计日志和越权失败策略；无则写“当前阶段无已确认要求”。】

| API-ID | 鉴权 | 空间 / 租户边界 | 资源权限 | 敏感字段 | 限流 | 审计日志 | 越权失败策略 |
|---|---|---|---|---|---|---|---|
| API-001 |  |  |  |  |  |  |  |

## 8. 异步任务 / 状态机

【撰写提要：接口涉及文件上传、导入 / OCR、RAG / LLM、外部服务调用或异步任务时，定义状态、进入 / 退出条件、可重试和终态；不适用则写“不适用”。】

| 状态 | 含义 | 进入条件 | 退出条件 | 用户可见信息 | 可重试 | 终态 |
|---|---|---|---|---|---|---|
| pending |  |  |  |  | 是 / 否 | 否 |

## 9. 兼容性与版本演进

【撰写提要：说明接口变更、版本兼容、弃用策略和客户端影响。】

- 兼容原则：
- 破坏性变更处理：
- 弃用策略：

## 10. API ↔ DB / Service / Test 追溯

【撰写提要：记录 API 与 Service / Command、数据来源、权限规则、错误码和 TC 的映射；权限边界必须在 API / Service / DB 查询边界体现。】

| API-ID | Service / Command | 数据来源 / 表 | 权限规则 | 错误码 | 关联测试 / TC | 状态 |
|---|---|---|---|---|---|---|
| API-001 |  |  |  | VALIDATION_ERROR | TC-001 |  |

| 权限场景 | DB 过滤 / 约束 | API 校验 | 错误码 | 测试 / TC |
|---|---|---|---|---|
|  |  |  |  |  |

## 11. REQ → 接口追溯矩阵

【撰写提要：填写本章节的项目事实；依据上游文档和对应 ai/doc-standards，不得新增未确认内容。】
| REQ / NFR | API-ID | 覆盖说明 | 阶段 | 状态 |
|---|---|---|---|---|
| REQ-001 | API-001 |  | [P1] |  |

## 12. 待人工确认项

【撰写提要：记录本阶段仍需人工确认的事项；必须包含 AI 建议、建议依据、备选方案和取舍影响，确认前不得写成事实。】
> 待确认项必须保留 AI 建议、建议依据、备选方案和取舍影响；AI 建议不等于用户确认。

| ID | 待确认项 | AI 建议 | 建议依据 | 备选方案 | 取舍影响 / 阻塞关系 |
|---|---|---|---|---|---|
| C-001 |  |  |  |  |  |
