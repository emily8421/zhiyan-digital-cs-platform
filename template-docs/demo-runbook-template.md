# <项目> 演示效果手册（demo runbook）

> Sync notice: This file is maintained by `ai-project-template` and may be overwritten when a derived project syncs template methodology.
> Do not edit it directly in derived projects; propose reusable changes in `_proposals/` and upstream them to the template repository.

> 定位：本文件是当前项目的演示 SOP，说明如何启动、检查和查看效果。**不替代 `docs/09-verification.md` 正式验收记录**。具体端口、脚本、二维码由项目填写；模板只规定结构。默认路径 `docs/env/local-demo-runbook.md`，多入口项目可按入口命名（如 `docs/env/admin-demo-runbook.md`）。

## 1. 当前适用范围

> 【撰写提要：当前阶段、交付物形态、验收依据、Mock / 降级 / 真实能力边界。】

- 适用阶段：
- 交付物形态：
- 验收依据：
- Mock / 降级 / 真实能力边界：

## 2. AI 场景：查看演示效果

> 【撰写提要：触发词、AI 执行流程、输出模板、需确认动作、禁止事项。对照 `ai/commands/show-demo.md` 执行边界表。】

- 触发词：
- AI 执行流程：
- AI 输出模板：
- 需用户确认的动作：
- 禁止事项：

## 3. 启动前提

> 【撰写提要：运行目录、必备工具、依赖安装状态、端口占用预检、本机 / 服务器 / 容器 / 外部服务边界。】

- 运行目录：
- 必备工具：
- 依赖安装状态：
- 端口占用预检：列出默认前端 / 后端端口；若被占用，停止占用进程或显式指定备用端口，不依赖开发服务隐式漂移。
- 本机 / 服务器 / 容器 / 外部服务边界：

## 4. 启动方式

> 【撰写提要：一键启动、手动启动、参数说明、关闭方式、strict port / 等价配置、后端代理地址注入方式。】

- 一键启动：
- 手动启动：
- 参数说明：默认端口只是示例；实际入口以启动脚本输出或运行状态文件为准。
- 端口策略：前端开发服务应启用 strict port 或等价机制；使用备用端口时，脚本必须同步更新输出 URL 和代理配置。
- 后端代理：记录前端 `/api` 代理目标（如 `DEMO_BACKEND_PROXY_URL` / 项目化环境变量），避免后端改端口后前端仍请求默认端口。
- 关闭方式：

## 5. 访问入口

> 【撰写提要：本机 URL、局域网 / 公网 URL、二维码 / 深链 / CLI 输出、API / 管理后台 / 移动端入口、页面 identity marker、运行状态文件。】

- 本机 URL：区分默认示例端口和本次运行实际端口；演示人员应优先使用启动脚本输出的 URL。
- 局域网 / 公网 URL：
- 二维码 / 深链 / CLI 输出：
- API / 管理后台 / 移动端入口：
- 页面身份标记：每个前端入口应提供项目专属 identity marker（如 `<meta name="<project>-demo-app" content="<entry>" />`），用于检查脚本确认访问的是当前项目页面。
- 运行状态文件：如项目提供启动脚本，建议写入 `.ai/local-demo-runtime.json`，记录本次端口、URL、LAN URL、二维码路径和进程 ID；该文件不得提交。

## 6. 检查与验证

> 【撰写提要：健康检查命令、自动检查脚本、页面身份校验、前端代理 API 检查、期望结果、常见失败与排查。演示检查 ≠ 正式验收。】

- 健康检查命令：
- 自动检查脚本：不得只检查 HTTP 200；必须校验页面 identity marker，并至少访问一个只读 `/api` 代理路径（如适用）确认前后端端口一致。
- 期望结果：页面身份匹配当前项目 / 当前入口，代理 API 可达；若端口返回 200 但 identity marker 不匹配，应判定为端口被其他本地应用占用。
- 常见失败与排查：端口占用、隐式端口漂移、代理仍指向默认后端端口、打开了其他项目页面、二维码或外部演示脚本引用了过期端口。

## 7. 推荐演示路径

> 【撰写提要：用户视角路径、管理端 / 运营端路径、Mock / 降级提示、结果观察点。】

- 用户视角路径：
- 管理端 / 运营端路径：
- Mock / 降级提示：
- 结果观察点：

## 8. 安全与边界

> 【撰写提要：不处理真实敏感数据、不启用未确认外部服务、不安装依赖 / 不产生费用、运行产物忽略规则。】

- 不处理真实敏感数据：
- 不启用未确认外部服务：
- 不安装依赖 / 不产生费用：
- 运行产物忽略规则（`.gitignore`）：`.ai/local-demo-runtime.json`、二维码、临时日志、端口状态文件等本地产物不得提交。

> 演示检查通过可作为 Sprint 总结的辅助证据；是否正式验收通过仍以 `docs/09-verification.md` 为准。若演示路径与正式文档不一致，应先修正文档或脚本，不得直接把演示事实写成需求事实。
