# MiniTodo（参考样例）

> 这是一个**填好的参考样例**，演示模板文档体系实际填完的样子。仅供对照学习，不是可运行项目（代码省略，只展示文档）。

- **项目**：MiniTodo —— 极简单人待办清单 API
- **形态**：全栈（FastAPI + SQLite），有数据库（用 docs/06）、有 API（用 docs/07）
- **对照**：本目录结构对应模板根目录——`docs/00-09` 为填好的实例，`ai/project-rules.md` 为填好的项目专属规则
- **未复制**：`ai/global-rules.md`、`ai/index.md` 与模板逐字相同（通用层），这里不重复；`docs/06、07` 因本项目有 DB/API 而**保留**

阅读建议：先看 `OVERVIEW.md`，再按 `docs/00-scenario` → `02-srs` → `03-09` 阅读，重点关注 `docs/06-db-design.md`、`docs/07-api-spec.md` 与 `docs/09-verification.md` 如何形成 DB/API/验收闭环，最后看 `ai/project-rules.md` 如何锁定 Phase 边界与技术栈。
