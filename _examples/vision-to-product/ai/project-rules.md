# 项目专属规则

> 本文件每个新项目都需要重新填写，不参与跨项目同步。
> 判断标准：一条规则换到另一个完全不同的项目上是否还成立——
> 不成立（涉及具体技术栈/具体功能/具体Phase定义）就属于本文件。
>
> 填写时机：§1 Phase边界、§2 技术栈、§3 项目形态与文档裁剪在生成 docs/03-09 **之前**填
> （作为约束）；§4 目录特例、§5 编码约定与禁区在审核 03-09 **之后**补。

## 初始化必填检查（生成 docs/03-09 前）

在使用 `ai/prompts/docs/01-review-inputs.md` 评审输入材料并用 `ai/prompts/docs/00-generate-or-complete-docs.md` 生成或补全 `docs/03-09` 前，必须确认以下项目已填写，不得保留占位说明直接进入设计阶段：

- `项目名称` 与 `代号/缩写` 已明确；若暂不需要缩写，写“无”。
- `§1 Phase边界` 已明确当前阶段允许、禁止与下一阶段预告；禁止项不得留空。
- `§2 技术栈约束` 已列出本项目确定使用的主要技术；不确定版本写“待确认”，不得虚构。
- `§2.5 运行环境与资源约束` 已说明 `docs/env/local-env.md` 当前状态、Demo 本机边界、可 Mock / 远程运行项和服务器预案；示例项目可标“待实际派生项目采集”。
- `§3 项目形态与文档裁剪` 已明确持久化、对外接口、`docs/06`、`docs/07` 与需要保留的代码目录。
- 不适用的模板目录或文档已有“保留 / 省略 / 删除”决策；省略 `docs/06` 或 `docs/07` 时必须在 §3 留下说明。
- 新增项目文档的类型与路径已按 `docs/README.md` 分区规则判断；不得把新增文档直接放到 `docs/` 根目录。
- 若以上任一项无法判断，AI 必须先向用户提问或提出待确认项，不得继续生成后续设计文档。

## 0. 项目标识

项目名称：Knowledge Companion
代号/缩写：kc

## 1. Phase边界

当前阶段：Phase1

允许：
- 本地单人使用
- CLI 命令入口
- SQLite 本地持久化
- 使用可替换的摘要服务接口；Phase1 可先用规则摘要或 mock 摘要实现
- 关键词搜索、列表查看、详情查看

禁止：
- 登录、多用户、云同步
- 向量数据库、语义检索、复杂 Agent 编排
- 标签体系、Markdown 导出、多项目知识库
- 后端 Web 服务、浏览器前端
- 未经确认引入第三方 AI SDK 或在线模型依赖

下一阶段预告：
- Phase2：标签、多项目知识库、Markdown 导出、可选语义检索

## 2. 技术栈约束

- 确定：Python 3.11 / SQLite（标准库 `sqlite3`）/ 标准库 `argparse` / 标准库 `pathlib`
- 摘要能力：Phase1 默认规则摘要或 mock 摘要；接入真实模型前必须人工确认供应商、密钥管理与费用风险
- 禁止引入：Web 框架、ORM、向量数据库、后台任务队列、未经确认的 AI SDK

## 2.5 运行环境与资源约束

- 本机环境文档：示例项目不采集真实机器信息；派生项目需运行 `scripts/collect-env.ps1` 生成 `docs/env/local-env.md`。
- Demo 阶段必须能在本机运行的部分：CLI、SQLite 本地数据库、规则摘要 / mock 摘要。
- 允许降级 / Mock / 远程运行的部分：真实 AI 摘要、语义检索。
- 禁止在本机运行的重资源部分：向量数据库、本地大模型推理。
- 是否允许使用公司服务器：示例项目不启用；派生项目待确认。
- 若需服务器，资源申请口径：仅当接入真实模型或语义检索时评估 CPU、内存、GPU / 显存、磁盘与端口需求。
## 3. 项目形态与文档裁剪

- 是否有持久化存储：有（SQLite 本地文件）
- 是否有对外接口：有（CLI 命令）
- docs/06-db-design.md：保留
- docs/07-api-spec.md：保留，用于描述 CLI 命令 / 参数 / 输出契约
- 需要保留的代码目录：backend/ tests/ scripts/

按项目形态裁剪说明：
- 本项目有 SQLite 持久化，因此保留 `docs/06-db-design.md`
- 本项目有 CLI 对外接口，因此保留 `docs/07-api-spec.md`
- Phase1 无 Web UI，因此不创建 frontend/

## 4. 目录规范的项目特例

无浏览器前端、无 Docker 部署；Phase1 保留 backend/、tests/、scripts/。

## 5. 编码约定与禁区

> Phase 级功能禁止见 §1，技术栈替代品禁止见 §2，本节只管代码层。
> 每条尽量具体可执行；没有则写“无”，不要留空占位。

### 5.1 既有约定（新代码必须向其看齐）
- 命名：Python 文件、函数、变量使用 snake_case；类名使用 PascalCase
- 分层与目录：CLI 参数解析放 `backend/cli.py`；业务逻辑放 `backend/service.py`；数据访问放 `backend/repository.py`；数据库初始化放 `backend/db.py`
- 既有模式：CLI 层不直接写 SQL；所有数据库读写统一经 repository 层
- 错误处理 / 日志：CLI 参数错误返回非 0 退出码；用户可理解错误写 stderr；Phase1 不引入日志框架

### 5.2 禁区（未经人工确认不得触碰）
- 不得擅改的文件 / 模块：`ai/` 通用规则文件、数据库迁移 / 初始化逻辑
- 不得擅自引入的依赖：任何新依赖须先确认，尤其 AI SDK、ORM、Web 框架
- 不得自行实现的功能：登录、多用户、云同步、向量检索、标签、导出、Web UI
- 愿景 / 01 中的功能点不等于已批准实现；阶段归属以 `docs/03-prd.md` §3 路线图为准，编码以 §1 当前阶段为准

## 6. AI修改确认规则

- AI在进行任何文件新增、修改、删除、重命名、格式化、批量替换前，必须先说明计划、影响范围与预期变更文件，并等待用户明确确认后再执行。
- AI在运行任何可能写入文件、安装依赖、生成构建产物、修改配置或改变项目状态的命令前，必须先询问用户确认。
- 只读分析操作（如读取文件、搜索代码、查看 Git 状态）无需逐次确认，但不得借只读分析之名修改项目内容。
- 用户在单次消息中明确要求“直接修改”“执行修复”“不必确认”等同类授权时，仅对该次明确任务生效；后续新任务仍默认先确认再修改。
