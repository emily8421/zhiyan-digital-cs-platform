# 09 验证计划（Verification）

> 本文回答“怎么算对”。与 `docs/08-dev-plan.md` 正交：08 说明何时做，09 说明如何验收。

## 1. 测试策略

- 单元测试：覆盖摘要模块、repository 查询、新增和详情读取。
- 集成测试：通过 CLI 命令验证 add/search/show 的端到端行为。
- 持久化测试：使用临时 SQLite 文件，新增后重新初始化连接再读取。
- 边界测试：空标题、空正文、无匹配关键词、不存在 ID。
- 人工验收：按 CLI 示例执行命令，确认输出可读且符合契约。

## 2. REQ → 用例追溯矩阵

| REQ | 阶段 | 交付物形态 | 状态 | 用例 ID | 验证方式 | 通过标准 |
|---|---|---|---|---|---|---|
| REQ-001 | [P1] | Demo | P1-已设计 | TC-001 | CLI 集成测试 | `kc add` 输入标题和正文后返回新 ID，列表 / 详情可查 |
| REQ-002 | [P1] | Demo | P1-已设计 | TC-002 | 单元测试 + CLI 集成测试 | 新条目摘要非空且不超过 120 字 |
| REQ-003 | [P1] | Demo | P1-已设计 | TC-003 | CLI 集成测试 | `kc search <keyword>` 返回标题、摘要或正文命中的条目 |
| REQ-004 | [P1] | Demo | P1-已设计 | TC-004 | CLI 集成测试 | `kc show <id>` 输出标题、摘要、正文和创建时间 |
| REQ-005 | [P1] | Demo | P1-已设计 | TC-005 | 持久化测试 | 重新连接 SQLite 后仍可读取已新增条目 |
| REQ-006 | [P1] | Demo | P1-已设计 | TC-006 | CLI 契约测试 | add/search/show 参数、退出码、错误提示符合 `docs/07-api-spec.md` |
| REQ-101 | [P2] | MVP | 骨架 | TC-101 | 待 Phase2 细化 | 可按标签过滤 |
| REQ-102 | [P2] | MVP | 骨架 | TC-102 | 待 Phase2 细化 | 可切换项目知识库 |
| REQ-103 | [P2] | MVP | 骨架 | TC-103 | 待 Phase2 细化 | 可导出 Markdown |
| REQ-201 | [愿景] | 产品 | 骨架 | TC-201 | 待愿景阶段细化 | 可进行语义检索 |

## 3. 分阶段验证范围

- Phase1（Demo）：必须覆盖 REQ-001~REQ-006；不验证标签、多项目、导出、语义检索。
- Phase2（MVP）：在原位追加 REQ-101~REQ-103 的详细用例，不删除 Phase1 用例。
- 愿景阶段（产品）：在确认技术方案后补 REQ-201 的语义检索验证方式。

## 4. Sprint 验收对应

- Sprint1：TC-001、TC-002、TC-004、TC-005、TC-006 中 add/show 部分。
- Sprint2：TC-003、TC-006 中 search 部分。
## 5. 本机资源验证

- 启动验证：在 `docs/env/local-env.md` 描述的本机环境中运行 CLI 帮助命令、add、search、show。
- 资源记录：记录 SQLite 文件大小、测试数据量、命令耗时和内存占用是否在人工确认上限内。
- 降级验证：真实摘要未启用时，确认规则摘要 / mock 摘要路径可完成最小闭环。
