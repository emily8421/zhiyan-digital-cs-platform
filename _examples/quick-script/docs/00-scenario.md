# 00 Scenario（项目背景与场景）

> 从 `docs/vision/product-vision.md` 抽取。本文只描述背景、用户和场景。

- 背景：会议纪要常包含多余空行、混乱缩进和不统一的项目符号，人工整理重复且耗时。
- 目标用户：需要快速清理本地文本文件的个人用户。
- 典型场景：用户把会议软件导出的文本保存为 `raw.txt`，运行脚本生成 `clean.txt`，再分享给团队。
- 当前目标：Phase1 只完成最常见格式清理，不做行动项提取、AI 总结或复杂 Markdown 结构化。
